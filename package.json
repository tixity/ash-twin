{
  "name": "ash-twin",
  "private": true,
  "version": "0.0.1",
  "description": "Automated regression testing platform for SquareMaze — Playwright + TypeScript.",
  "type": "module",
  "scripts": {
    "t": "playwright test",
    "t:headed": "playwright test --headed",
    "t:ui": "playwright test --ui",
    "t:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "mysql2": "^3.11.5"
  }
}
